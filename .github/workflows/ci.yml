name: ci

on:
    push:
        branches:
            - main
    pull_request:

permissions:
    contents: read

concurrency:
    group: ci-${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    lint:
        runs-on: ubuntu-26.04
        timeout-minutes: 10
        steps:
            - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
            - name: Install Stylua
              run: cargo install stylua --version 2.5.2 --locked
            - name: Run lint checks
              run: scripts/ci/lint.sh
            - name: Validate workflow syntax
              run: go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.7

    test:
        name: test (${{ matrix.neovim }})
        runs-on: ubuntu-26.04
        timeout-minutes: 20
        strategy:
            fail-fast: false
            matrix:
                neovim: ['v0.11.5', 'stable', 'nightly']
        steps:
            - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
            - uses: rhysd/action-setup-vim@febef33995d6649302e9d88dda81e071b68f16a7 # v1
              with:
                  neovim: true
                  version: ${{ matrix.neovim }}
            - name: Run tests
              run: scripts/ci/test.sh
            - name: Run clean-install smoke
              run: scripts/ci/install-smoke.sh

    continuity-compatibility:
        runs-on: ubuntu-26.04
        timeout-minutes: 20
        env:
            CONTINUITY_REQUIRE_TERMINALIA_INTEGRATION: '1'
            TERMINALIA_NVIM_PATH: ${{ github.workspace }}
        steps:
            - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
            - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
              with:
                  repository: griwes/continuity.nvim
                  ref: main
                  path: .dependencies/continuity.nvim
            - uses: rhysd/action-setup-vim@febef33995d6649302e9d88dda81e071b68f16a7 # v1
              with:
                  neovim: true
                  version: stable
            - name: Run Continuity against this Terminalia checkout
              working-directory: .dependencies/continuity.nvim
              run: scripts/ci/test.sh

    ministry-compatibility:
        runs-on: ubuntu-26.04
        timeout-minutes: 20
        env:
            MINISTRY_NVIM_PATH: ${{ github.workspace }}/.dependencies/ministry.nvim
            TERMINALIA_REQUIRE_MINISTRY_INTEGRATION: '1'
        steps:
            - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
            - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
              with:
                  repository: griwes/ministry.nvim
                  ref: main
                  path: .dependencies/ministry.nvim
            - uses: rhysd/action-setup-vim@febef33995d6649302e9d88dda81e071b68f16a7 # v1
              with:
                  neovim: true
                  version: stable
            - name: Run Terminalia's Ministry integration gate
              run: scripts/ci/test.sh
