paths:
    .github/workflows/**/*.{yml,yaml}:
        ignore:
            # rhysd/actionlint#683 fixes ubuntu-26.04 recognition; remove this when released.
            - 'label "ubuntu-26\.04" is unknown'
